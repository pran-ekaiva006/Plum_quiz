# Set to 'true' to use deterministic local mocks (no network needed)
VITE_USE_MOCK=true

# If using a real AI backend, set these:
# VITE_AI_ENDPOINT=https://your-server.example.com/generate
# VITE_AI_API_KEY=sk-...
